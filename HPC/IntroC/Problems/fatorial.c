#include <stdio.h>
#include <stdlib.h>

int fatorial(int n) {
    // seu código aqui
}

int main() {

    printf("O fatorial de 3 = %d\n", fatorial(3));
    printf("O fatorial de 10 = %d\n", fatorial(10));

    return(0);
}